{
  "name": "ab-testing-wordpress",
  "version": "0.1.0",
  "description": "WordPress plugin for A/B testing pages with internal tracking.",
  "private": true,
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:clean": "wp-env clean all",
    "env:cli": "wp-env run cli wp",
    "env:tests-cli": "wp-env run tests-cli wp"
  },
  "devDependencies": {
    "@wordpress/env": "^11.4.0"
  }
}
